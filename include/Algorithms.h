#include "Encodings.h"

namespace Algorithms {

// IAQ
std::vector<std::string> alg_iaq_cred_co(const AF & af);
std::vector<std::string> alg_iaq_cred_st(const AF & af);

// EEE
std::vector<std::string> alg_eee(const AF & af);

// SEE
std::vector<std::string> alg_see(const AF & af);

// SEEM
std::vector<std::string> alg_seem(const AF & af);

// fudge
std::vector<std::string> alg_fudge(const AF & af);

}